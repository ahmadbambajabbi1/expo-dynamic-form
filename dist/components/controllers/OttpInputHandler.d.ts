import { apiOptionsType } from "../../types";
type PropsType = {
    verifyingDataProps: any;
    apiOptions: apiOptionsType;
};
declare const OttpInputHandler: ({ verifyingDataProps, apiOptions }: PropsType) => JSX.Element;
export default OttpInputHandler;
